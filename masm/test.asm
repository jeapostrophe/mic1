backward: 0
    
    LODD 1
    LODD forward:
    LODD backward:

    STOD 2
    STOD forward:
    STOD backward:

    ADDD 3
    ADDD forward:
    ADDD backward:

    SUBD 4
    SUBD forward:
    SUBD backward:

    JPOS 5
    JPOS forward:
    JPOS backward:

    JZER 6
    JZER forward:
    JZER backward:

    JUMP 7
    JUMP forward:
    JUMP backward:

    LOCO 8
    LOCO forward:
    LOCO backward:

    LODL 9

    STOL 10

    SUBL 11

    JNEG 12
    JNEG forward:
    JNEG backward:

    JNZE 13
    JNZE forward:
    JNZE backward:

    CALL 14
    CALL forward:
    CALL backward:

    PSHI

    POPI

    PUSH

    POP

    RETN

    SWAP

    INSP 15

    DESP 16

    HALT

    -4000
    -4
    -1
    0
    1
    4
    4000
    
forward: 17

.LOC 80
    81

    "Odd"
    "Even"
